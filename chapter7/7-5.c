#include <stdlib.h>
#include <stdio.h>

#define MAX 1000
char line[MAX];

int main(int argc, char *argv[]) {
  // parse line
  scanf("%s\n", line);
  // get op code
  char c;
  scanf("%c", &c);

  // get 操作数
  int i;
  scanf("%d", &i);
  exit(0);
}
